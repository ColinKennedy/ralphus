[[task]]
name    = "RAL-417"
project = "ralphus"
agent   = "pi-openrouter-deepseek"

  [[task.cell]]
  id                     = "work"
  name                   = "RAL-417: work"
  cwd                    = "<<ralphus:new-worktree/RAL-417-resizable-reviews-sidebar?upstream=staging>>"
  triage                 = true
  system_prompt          = "Do NOT commit and do NOT push under any circumstances. Work only in the dedicated git worktree."
  system_prompt_position = "append"
  prompt                 = """
Your working directory is a git worktree checked out for this ticket's branch.

# RAL-417: Add resizer to reviews sidebar

Implement the ticket exactly as specified: add a persistent draggable Reviews-sidebar splitter independent of the Squads preference; use the established sidebar drag lifecycle and documented CSS variables; preserve at least 240px for the review detail pane; and make the Reviews view usable on small screens, including iPhones.

Modify the appropriate board HTML, CSS, and chrome JavaScript. Add the required pure/browser coverage, run relevant formatting, linting, and tests, and leave the intended source changes uncommitted for the finalize cell.
"""

    [[task.cell.proof]]
    id     = "web"
    prompt = "Run `npm install --no-audit --no-fund`, `npm run lint`, `npm run typecheck`, `npm run knip`, and the relevant board tests. Fix only task-caused failures and re-run. Do NOT commit or push. End with `RALPHUS_PROOF: PASS` only if all selected checks pass."

  [[task.cell]]
  id                     = "finalize"
  name                   = "RAL-417: finalize"
  cwd                    = "<<ralphus:new-worktree/RAL-417-resizable-reviews-sidebar?upstream=staging>>"
  depends_on             = ["work"]
  system_prompt          = "Stage only the relevant source files, commit them, and push the commit if a remote exists. Do not run checks."
  system_prompt_position = "append"
  prompt                 = "Stage only the relevant source files, commit them, and push the commit if a remote exists."

[[task]]
name    = "RAL-419"
project = "ralphus"
agent   = "pi-openrouter-deepseek"

  [[task.cell]]
  id                     = "work"
  name                   = "RAL-419: work"
  cwd                    = "<<ralphus:new-worktree/RAL-419-persistent-graph-selection?upstream=staging>>"
  triage                 = true
  system_prompt          = "Do NOT commit and do NOT push under any circumstances. Work only in the dedicated git worktree."
  system_prompt_position = "append"
  prompt                 = """
Your working directory is a git worktree checked out for this ticket's branch.

# RAL-419: Restore squad graph selection state

Implement the ticket exactly as specified. Persist each squad's full primary and multi-selection across browser refreshes; restore it when returning; honor explicit sidebar selection and authoritative hash navigation; reconcile stale selections to the nearest surviving parent; and keep multi-selection separate from primary-selection semantics.

Add the requested pure marker-extracted transition/reconciliation test and browser-manual coverage. Run relevant formatting, linting, and tests, and leave intended source changes uncommitted for the finalize cell.
"""

    [[task.cell.proof]]
    id     = "web"
    prompt = "Run `npm install --no-audit --no-fund`, `npm run lint`, `npm run typecheck`, `npm run knip`, and the relevant board tests. Fix only task-caused failures and re-run. Do NOT commit or push. End with `RALPHUS_PROOF: PASS` only if all selected checks pass."

  [[task.cell]]
  id                     = "finalize"
  name                   = "RAL-419: finalize"
  cwd                    = "<<ralphus:new-worktree/RAL-419-persistent-graph-selection?upstream=staging>>"
  depends_on             = ["work"]
  system_prompt          = "Stage only the relevant source files, commit them, and push the commit if a remote exists. Do not run checks."
  system_prompt_position = "append"
  prompt                 = "Stage only the relevant source files, commit them, and push the commit if a remote exists."

[[task]]
name    = "RAL-420"
project = "ralphus"
agent   = "pi-openrouter-deepseek"

  [[task.cell]]
  id                     = "work"
  name                   = "RAL-420: work"
  cwd                    = "<<ralphus:new-worktree/RAL-420-squad-generation-costs?upstream=staging>>"
  triage                 = true
  system_prompt          = "Do NOT commit and do NOT push under any circumstances. Work only in the dedicated git worktree."
  system_prompt_position = "append"
  prompt                 = """
Your working directory is a git worktree checked out for this ticket's branch.

# RAL-420: Account for squad generation cost

Implement the ticket exactly as specified. Retain usage for all pre-work agent/model calls, durably attribute it to a submitted squad as a distinct squad-owned category, include it exactly once in normal squad totals, expose audit/detail data, and log the required Cartographer and stderr events. Retain partial cancelled or failed generation cost when available and mark estimated snapshots appropriately.

Do not repurpose the daemon-global Arbiter ledger. Add migration-safe persistence and focused tests for attribution, non-submission/cancellation, reload, logging, and mixed total aggregation. Run relevant Rust and web checks, observing the dogfooding restriction against all-targets test commands, and leave intended source changes uncommitted for the finalize cell.
"""

    [[task.cell.proof]]
    id     = "rust"
    prompt = "Run `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`, and focused daemon tests. Do not run `cargo test --all-targets` or `cargo nextest run --all-targets`; use `cargo nextest run -p ralphus-daemon --lib` for daemon coverage. Fix only task-caused failures and re-run. Do NOT commit or push. End with `RALPHUS_PROOF: PASS` only if all selected checks pass."

    [[task.cell.proof]]
    id     = "web"
    prompt = "Run `npm install --no-audit --no-fund`, `npm run lint`, `npm run typecheck`, `npm run knip`, and relevant board tests. Fix only task-caused failures and re-run. Do NOT commit or push. End with `RALPHUS_PROOF: PASS` only if all selected checks pass."

  [[task.cell]]
  id                     = "finalize"
  name                   = "RAL-420: finalize"
  cwd                    = "<<ralphus:new-worktree/RAL-420-squad-generation-costs?upstream=staging>>"
  depends_on             = ["work"]
  system_prompt          = "Stage only the relevant source files, commit them, and push the commit if a remote exists. Do not run checks."
  system_prompt_position = "append"
  prompt                 = "Stage only the relevant source files, commit them, and push the commit if a remote exists."

[[task]]
name    = "RAL-421"
project = "ralphus"
agent   = "pi-openrouter-deepseek"

  [[task.cell]]
  id                     = "work"
  name                   = "RAL-421: work"
  cwd                    = "<<ralphus:new-worktree/RAL-421-triage-confirmation-and-grouping?upstream=staging>>"
  triage                 = true
  system_prompt          = "Do NOT commit and do NOT push under any circumstances. Work only in the dedicated git worktree."
  system_prompt_position = "append"
  prompt                 = """
Your working directory is a git worktree checked out for this ticket's branch.

# RAL-421: Confirm and partition triage reviews

Implement the ticket exactly as specified. Add a non-mutating rough preview for a proposed threshold; require immediate UI confirmation for both editor entry points before any mutation or LLM work; atomically persist and drain each eligible pool in threshold-sized batches after confirmation; and preserve complete pool identity across cron and threshold paths so no reviews cross project, subproject, or type boundaries.

Do not create a pending-confirmation lifecycle. Explicitly decide and document the CLI threshold semantics. Replace the old cross-subproject straggler-sweep expectations with isolation regressions. Add focused coverage for preview/cancel/confirm, batching, concurrent empty-pool safety, partitioning, and both UI entry points. Run relevant Rust and web checks, observing the dogfooding restriction against all-targets test commands, and leave intended source changes uncommitted for the finalize cell.
"""

    [[task.cell.proof]]
    id     = "rust"
    prompt = "Run `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`, and focused daemon tests. Do not run `cargo test --all-targets` or `cargo nextest run --all-targets`; use `cargo nextest run -p ralphus-daemon --lib` for daemon coverage. Fix only task-caused failures and re-run. Do NOT commit or push. End with `RALPHUS_PROOF: PASS` only if all selected checks pass."

    [[task.cell.proof]]
    id     = "web"
    prompt = "Run `npm install --no-audit --no-fund`, `npm run lint`, `npm run typecheck`, `npm run knip`, and relevant board tests. Fix only task-caused failures and re-run. Do NOT commit or push. End with `RALPHUS_PROOF: PASS` only if all selected checks pass."

  [[task.cell]]
  id                     = "finalize"
  name                   = "RAL-421: finalize"
  cwd                    = "<<ralphus:new-worktree/RAL-421-triage-confirmation-and-grouping?upstream=staging>>"
  depends_on             = ["work"]
  system_prompt          = "Stage only the relevant source files, commit them, and push the commit if a remote exists. Do not run checks."
  system_prompt_position = "append"
  prompt                 = "Stage only the relevant source files, commit them, and push the commit if a remote exists."
