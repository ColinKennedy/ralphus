[package]
name = "ralphus-bench-types"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "BenchMeta: the shared, dependency-free type RAL-94 bench-tagged crates and the harness both link against."

[lib]
name = "ralphus_bench_types"
path = "src/lib.rs"

[lints]
workspace = true
