# bench-types/

`ralphus-bench-types` holds only `BenchMeta` — kept dependency-free to avoid a
cyclic dependency between bench-tagged crates and the harness (RAL-94). Full
harness design and the patience-authoring rule live in
[[../bench-harness/AGENTS|bench-harness/AGENTS.md]].
