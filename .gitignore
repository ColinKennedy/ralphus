# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/
dist/
build/

# ralphus runtime state (daemon DB, pid files, logs)
*.db
*.db-wal
*.db-shm
*.pid
/.ralphus/
# Guardian review worktrees and task worktrees created at runtime
/.ralphus_guardian/
/.worktrees/

# Node (only for linting board.html's inline JS — see package.json)
node_modules/

# Secure distribution — private key must never be committed
ralphus-private.key
ralphus.lic

# Editors / OS
.DS_Store
*.local.md.bak
