{
  "//": "Type-checks the board chunk files' JSDoc annotations — no .ts files are compiled or shipped. The chunks are plain scripts sharing one global scope (loaded via board.html's sequential <script> tags), so declarations in one chunk resolve in all others.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "types": []
  },
  "include": ["librarian/assets/board/**/*.js"]
}
