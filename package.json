{
  "name": "ralphus-web-lint",
  "private": true,
  "description": "Lint tooling for the librarian's board chunk JavaScript. Not a runtime dependency — the librarian embeds the board assets via include_str! (see librarian/build.rs) and ships no Node code.",
  "scripts": {
    "lint": "node scripts/generate-board-globals.mjs && eslint \"librarian/assets/board/**/*.js\"",
    "typecheck": "tsc -p tsconfig.board.json",
    "knip": "node scripts/knip-board.mjs",
    "test": "node --test \"test/*.test.mjs\""
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "eslint-plugin-jsdoc": "^50.4.3",
    "globals": "^15.11.0",
    "knip": "^5.33.3",
    "typescript": "^5.6.3"
  }
}
