"""ralphus — CLI for submitting and managing autonomous agent tasks.

The CLI is a thin client over the daemon's HTTP/JSON API; it holds no state of
its own. See ``docs/daemon-api.md`` for the contract.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
